import PlayButton from "../buttons/PlayButtont";

const PlaylistCommandButtons: React.FC = () => {
  return (
    <div className="flex flex-row p-5">
      <PlayButton buttonSize="lg" iconSize="lg" onClick={() => {}} />
    </div>
  );
};

export default PlaylistCommandButtons;
